doctype html
html(lang="ko")
    head
        title #{pageTitle} | jsToDo
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            h1=pageTitle
            nav
                ul
                    li
                        a(href="/") Home
                    li
                        a(href="/videos/upload") push
                            
        main 
            block content
    include partials/footer.pug